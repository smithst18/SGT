import  { User as userModel }  from "./user";
import { Ticket as ticketModel } from "./ticket";
import { Entity as entityModel } from "./entity";
import { Position as positionModel } from "./position";
import { Chat as chatModel} from "./chat";
import { Survey as surveyModel } from "./survey";

export { 
    userModel,
    ticketModel,
    entityModel,
    positionModel,
    chatModel,
    surveyModel
}