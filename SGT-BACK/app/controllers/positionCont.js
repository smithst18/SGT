import  { entityModel }  from "../models";
import { matchedData } from "express-validator";
import { handleError } from "../helpers/handleHttpErrors";

/**
 * endopoint to save and user position at work
 * @param {*} req 
 * @param {*} res 
 */
export const savePosition = (req, res) =>{
    
} 